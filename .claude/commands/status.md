---
description: Produce a phase-gate status summary from STATUS.md, METRICS.md, and git
---
Summarize current project status.

1. Read `STATUS.md` (phase + next actions), the last 10 rows of `METRICS.md`, and the
   recent `git log`.
2. Produce: current phase, last results (with evidence and file paths), the open gate,
   the top risks, and the exact next action. Evidence over adjectives; bad news first.
